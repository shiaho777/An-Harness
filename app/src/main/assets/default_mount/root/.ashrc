# Interactive ash configuration
# Sourced via ENV=$HOME/.ashrc set in /etc/profile.d/minis.sh.

# Common quality-of-life aliases.
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ..='cd ..'
alias ...='cd ../..'
